# Native projects are COMMITTED (hand-applied permissions live in them — a
# `npx cap add` regen silently wipes those; don't regenerate, sync instead).
# Build artifacts are excluded by ios/.gitignore and android/.gitignore.

# Dependencies
/node_modules/
package-lock.json

# Build / tooling
.DS_Store
*.log
